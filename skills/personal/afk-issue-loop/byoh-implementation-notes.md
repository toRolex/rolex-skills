# BYOH 文档改写记录

## 会话复盘后的收敛

用户要求从最早 Git 历史恢复直接、清晰的用法，并允许优化整个 skill 目录。本次亲自重写主文档、Reference、示例、三个角色模板和现场说明；脚本仅标明已有 v2 记录用途，不改算法。

- 阅读最早 `9351dbd` 的主文档、Reference 和 Implementer，确认最初结构是当前 agent 读票后直接分派。未照搬其文本依赖、自动重派或实现者自行合并。
- 阅读 `b2de68f` 的逐票实现→审查、批末合并；追踪 `2149811` 加入主窗口预算、固定通知和 plan 落盘，以及 `8d222a2` 加入强制校验、计数和 watchdog。恢复业务主干，不恢复这些平台前置。
- 对照用户指定 Claude/Pi 会话：收回新增调度者许可；父 SPEC 与执行依赖分开；只读 EOF/超时最多再试两次；用户重新开始不恢复已停止代理。
- 取消正常启动必须生成 v2 plan/dispatch、调用状态校验的要求。当前 agent 用简短持久化记录随执行更新，保留身份关联、固定批次、原始基线、审查/测试 SHA、跳过集合和现场占用事实。
- 当前 agent 直接派 Implementer，逐票独立 Reviewer，本批结束派一个 Merger。宿主所需调用脚本可保留，但不增加 Planner、准备、记录或调度代理。
- 仅修改本 skill。目录外 README/router/CONTEXT/ADR 未同步；旧脚本保留用于已有记录，既有实现记录保留历史事实。未接管旧会话、未启动业务、未运行测试、未提交或推送。
- 按 writing-for-agents 再次整理：主文保留选四票、直接分派、逐票审查、批末合并四步，各自带完成条件；父 SPEC、读取故障、旧现场按条件进入 Reference。测试规则有独立锚点，角色完成条件只绑定自身阶段，不依赖后续审查或合并。
- 第一位审查者未发现确定错误；独立功能审查随后指出只能最终返回时的 Merger 关闭衔接、唯一证据留存、无变化实现归类及免测歧义，已修正并通过定点复核。该复核又发现角色完成条件误引用全流程交付要求，已拆开并经最后定点确认关闭。
- 静态检查包括本地文件与标题链接、diff 空白、脚本逻辑与执行权限；脚本只有注释变化。文档缩短不作为正确性证据，静态走查也不等于 harness 实测。

以下保留第一次 BYOH 改写的记录，其中允许调度职责迁移和强制记录接口的描述已由上述决定取代。

## 首次 BYOH 改写

- 主流程按业务动作描述：读取依赖、固定最多四票真实并发实现、每票立即独立审查、全批结果明确后统一顺序合并、核对刷新下一批。
- 调度者改为唯一逻辑职责，可在已授权执行上下文运行；三个角色是职责模板，不绑定注册类型、工具名、父 session 或工具调用次数。
- 工作执行、结果获取和记录持久化采用宿主已有方式。RESULT_PATH 指定单一授权持久化所有者；角色返回内容与记录步骤可顺序衔接，证据保存并核对后才关闭 Issue。
- 路径按实际执行环境解析，材料通过已授权方式提供，不假设共享文件系统。Skill 工具有则使用，无则通过宿主支持方式加载同一材料。

## 保留不变量

固定批次、不补位、独立 Reviewer、同现场单写者及退出证据、调度状态单写入权、失败本次跳过、有限观察与未知现场冻结均保留。GitHub 原生依赖、Worktrunk 生命周期规则、SHA 与 RESULT_PATH 数据接口、交付/关闭/清理分账、权限边界、不 push 或创建 PR 均不变。

## 范围与偏离

仅修改本 skill 的七份操作文档并新增本记录。既有 IMPLEMENTATION-NOTES.md 保留历史事实；代码块、JSON、其余 YAML、原有链接目标及 scripts 不变，未编辑目录外文件。

经主代理补充决定，唯一元数据调整为 argument-hint 收窄到 `[issue-number ...]`，属于 BYOH 功能调整而非纯润色。旧 `mode=subagent|herdr` 只作历史偏好，不再定义入口选项、工具或执行拓扑；默认依宿主已有委派方式，不建立新的供应商模式枚举。

保留的具体绑定是项目操作及数据约定，包括 GitHub/gh、Worktrunk、分支和目录检查命令、内部 v2 schema 及脚本路径。本次没有开发运行时适配层或通用消息总线，也没有改动历史交接所述会话。

## 静态核对与未实测

完成限定目录的 git diff --check，并静态核对代码块、元数据和原有链接目标的保留情况。顾问复核未发现确定的文本矛盾；这次结论仅针对文档中的职责、授权、材料传递和记录所有权。

未研究供应商 API、运行测试或业务脚本，未创建 worktree、提交或 push。Pi、Claude Code、Codex、OpenCode 的实际执行、材料传递、退出证明及记录持久化均未实测；文档示例不代表兼容性验证。
